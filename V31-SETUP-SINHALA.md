# Elishah Rides V31 — Booking + Payment Flow

මෙම update එකෙන් Booking section එක multi-step luxury flow එකක් කරයි.

1. `index.html`, `styles.css`, `script.js` files 3ම ඔබගේ existing project root එකේ files 3 replace කරන්න.
2. `.git` folder එක touch/delete කරන්න එපා.
3. VS Code save කරන්න.
4. CMD එකේ project root එකට ගිහින්:

```cmd
git add .
git commit -m "Add animated booking payment flow"
git push origin main
```

5. Vercel deploy complete වුණාම site එකට `Ctrl + Shift + R` කරන්න.

V31 features:
- Step 1: Journey details
- Step 2: Review & Payment
- Bank Transfer / PayPal choice
- Animated review and confirmation
- Booking reference number
- WhatsApp confirmation CTA
- Mobile responsive
- Reduced-motion accessibility support
- Existing Supabase booking + vehicle availability flow preserved
